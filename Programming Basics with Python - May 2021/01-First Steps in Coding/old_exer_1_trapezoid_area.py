side_a=float(input('Enter first side length '))
side_b=float(input('Enter second side length '))
height_c=float(input('Enter height length '))

area_trapezoid=(side_a+side_b)*height_c/2

print(f'The trapezoid area is {area_trapezoid} cm2')